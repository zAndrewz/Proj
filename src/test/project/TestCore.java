/*
package project;

import org.junit.After;
import org.junit.Before;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import BasePage;
import GooglePage;
import LoginPage;
import MessageWindowPage;

import java.io.IOException;
import java.util.concurrent.TimeUnit;

import static org.junit.Assert.fail;

public class TestCore {
  //  protected Logger logger;

    //private boolean acceptNextAlert = true;
    private StringBuffer verificationErrors = new StringBuffer();


//    public LoginHelper loginHelper;
//    public MainMenuHelper mainMenuHelper;
//    public ConterpartyHelper conterpartyHelper;
//    public DocumentsHelper documentsHelper;
//    public PaymentOrderHelper paymentOrderHelper;

    public TestCore() {
//        this.logger = new LoggerConsole();
        //this.logger = new LoggerFile("D:/learn_at/logs/test_log.txt");
    }

    /*public void auth(String userLogin, String password) {
        driver.get("http://192.168.14.7:8080/reckondemo/login.php");
        editHelper.fillEdit(By.id("login"), userLogin);
        editHelper.fillEdit(By.id("password"), password);
        buttonHelper.clickButton(By.id("submit"));
    }*/
/*
    public void shutdown() {
        driver.quit();
        String verificationErrorString = verificationErrors.toString();
        if (!"".equals(verificationErrorString)) {
            fail(verificationErrorString);
        }
    }

 //   @Before
    public void setUp() throws Exception {
        init();
    }

 //   @After
    public void tearDown() throws Exception {
        shutdown();
        //logger.write
    }

//    public void init() throws IOException {
//        System.setProperty("webdriver.chrome.driver", "D:\\IntelliJProj\\chromedriver.exe");
//        driver = new ChromeDriver();
//        driver.manage().window().fullscreen();
//        driver.manage().window().maximize();
//        driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);
//
//
//        driver.get("https://www.google.com/gmail/");
//    }


//        loginHelper = new LoginHelper(driver, logger);
//        mainMenuHelper = new MainMenuHelper(driver, logger);
//        conterpartyHelper = new ConterpartyHelper(driver, logger);
//        documentsHelper = new DocumentsHelper(driver, logger);
//        paymentOrderHelper = new PaymentOrderHelper(driver, logger);


        //loginHelper.login("testuser101", "12345123456");
    }
}
        */